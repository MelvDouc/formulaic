{
  "name": "shape-and-form",
  "version": "1.0.6",
  "description": "A lightweight form validator.",
  "engines": {
    "node": ">=14.17.0"
  },
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "npx tsc && npx tsc-alias",
    "prepublishOnly": "npm run build",
    "test": "npm run build && node --test dist"
  },
  "keywords": [
    "form",
    "validator",
    "validation"
  ],
  "author": "MelvDouc",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.4.9",
    "tsc-alias": "^1.8.7",
    "typescript": "^5.1.6"
  }
}